'''
2. Cree una función que le de la vuelta a un string y lo retorne.
    1. Esto ya lo hicimos en iterables.
    2. “Hola mundo” → “odnum aloH”
    '''

input_text = input("write a sentence: ")

for char in input_text[::-1]:
    print(char)

for index in range(0, len (input_text)):
    combined_list.append(input_text[:])
    print(input_text, end="")